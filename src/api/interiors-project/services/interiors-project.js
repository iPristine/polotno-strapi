'use strict';

/**
 * interiors-project service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::interiors-project.interiors-project');
